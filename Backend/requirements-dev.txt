# Dependencias solo para desarrollo / testing.
# Instalar con: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2  # cliente que usa fastapi.testclient internamente
locust==2.32.4  # para pruebas de carga
